# 🎮 Проект: Fruit Ninja

## 👤 Информация
**Ученик:** _Александр Трифонов_  
**Курс:** _[Создание 3D-игр в Unity + C#](https://easypro.academy/courses/unity)_  
**Название проекта:** _Fruit Ninja_  

**Преподаватель:** [Дмитрий Курилов](https://github.com/Lalamee)

---

## 📌 Описание проекта

**Fruit Ninja** — 3D‑аркада, в которой игрок **разрезает летящие объекты еды** с помощью “свайпа” мышью и набирает очки.

В проекте реализованы:
- выбор сложности перед стартом;
- спавн объектов по таймеру;
- начисление очков за разрезание еды;
- система жизней (ошибка / пропуск объекта);
- пауза;
- Game Over и перезапуск.

---

## 🧩 Как работает игра

### 1) Старт и выбор сложности
На старте показывается меню с кнопками:
- **Easy**
- **Medium**
- **Hard**

Кнопка сложности запускает игру через `GameManager.StartGame(difficulty)`:
- увеличивает/уменьшает частоту появления объектов (через `spawnRate`);
- скрывает меню;
- включает игровой процесс.

### 2) Спавн объектов
В `GameManager` хранится список префабов:

- `List<GameObject> targets`

Запуск происходит корутиной `SpawnTarget()`:
- пока `isGameActive == true`  
  → ждать `WaitForSeconds(spawnRate)`  
  → выбрать случайный префаб из списка  
  → `Instantiate(...)`

### 3) Полёт и поведение объектов
За поведение каждого объекта отвечает `Target.cs`.

При создании объекта:
- берётся `Rigidbody`;
- задаётся случайная позиция по X;
- добавляется импульс вверх (`AddForce`);
- добавляется случайное вращение (`AddTorque`).

### 4) “Свайп” и разрезание
Игрок управляет объектом **Swiper Object** (скрипт `ClickAndSwipe.cs`):
- при зажатой **ЛКМ** включаются `TrailRenderer` и `BoxCollider`;
- объект следует за курсором (через `Camera.ScreenToWorldPoint`);
- при столкновении с объектом `Target` вызывается `target.DestroyTarget()`.

### 5) Очки и жизни
- Очки добавляются через `Score.AddScore(cost)`.
- Жизни уменьшаются через `Lives.DecreaseLives(1)`.

**Потеря жизни происходит в двух случаях:**
1) Игрок разрезал **Bad** объект  
   (в `Target.DestroyTarget()` проверяется `TryGetComponent<Bad>(...)`)
2) Игрок **пропустил Good объект**, и он столкнулся с нижним триггером  
   (`GameOverTrigger` + `GameOver.cs`, проверка `TryGetComponent<Good>(...)`)

---

## ⏸️ Пауза, Game Over и перезапуск

### Пауза
Клавиша **P** включает/выключает паузу:
- показывается/скрывается `pauseMenu`;
- меняется `Time.timeScale` (0 или 1).

### Game Over
`GameManager` постоянно проверяет количество жизней:
- если `lives <= 0` → `GameOver()`:
  - останавливает игру (`isGameActive = false`, `Time.timeScale = 0`);
  - показывает `GameOverText`;
  - включает кнопку `RestartButton`.

### Перезапуск
Кнопка перезапуска вызывает `RestartGame()`:
- `SceneManager.LoadScene(SceneManager.GetActiveScene().name)`

---

## 🛠️ Используемые технологии и навыки

| Тема | Где используется в проекте |
|---|---|
| **Unity 6000.3.3f1** | сцены, префабы, компоненты, инспектор |
| **C# / ООП** | классы `GameManager`, `Target`, `Score`, `Lives` и др.; поля/методы; инкапсуляция |
| **Коллекции** | `List<GameObject> targets` для хранения префабов и случайного выбора |
| **Корутины** | `IEnumerator SpawnTarget()` + `WaitForSeconds` для спавна по таймеру |
| **Физика** | `Rigidbody`, `AddForce`, `AddTorque`, `OnCollisionEnter`, `ForceMode.Impulse` |
| **UI (UGUI + TextMeshPro)** | `Canvas`, `Button`, `TextMeshProUGUI` (очки/жизни/надписи) |
| **Управление временем** | `Time.timeScale` (пауза и остановка на Game Over) |
| **Ввод пользователя** | `Input.GetMouseButtonDown/Up`, `Input.mousePosition`, `Input.GetKeyDown(KeyCode.P)` |
| **Рандомизация** | `Random.Range` (выбор префаба, позиция, сила, вращение) |
| **Работа со сценами** | `SceneManager` для перезапуска текущей сцены |
| **Атрибуты Unity** | `[SerializeField]`, `[RequireComponent]` |

---

## ▶️ Как запустить

1) Установите **Unity Hub** и Unity Editor версии **6000.3.3f1** (или совместимую Unity 6).  
2) Склонируйте/скачайте репозиторий.  
3) В Unity Hub нажмите **Add** и выберите папку проекта:  
   `My project`
4) Откройте сцену:  
   `Assets/Scenes/Prototype 5.unity`
5) Нажмите **Play**.

> При первом открытии Unity может предложить импортировать **TextMeshPro Essentials** — согласитесь.

---

## 🎮 Управление

- **ЛКМ (зажать)** — включить “свайп” (трейл + коллайдер)
- **Движение мыши** — движение “лезвия”
- **P** — пауза/продолжить
- **Easy / Medium / Hard** — старт игры с выбранной сложностью
- **Restart** — перезапуск после Game Over

---

## 📁 Структура проекта

- `Assets/Scenes/Prototype 5.unity` — основная сцена
- `Assets/MY FOLDER/Scripts/` — скрипты проекта:
  - `GameManager.cs` — спавн, пауза, game over, перезапуск
  - `DifficultyButton.cs` — кнопки сложности
  - `ClickAndSwipe.cs` — “свайп” и столкновения
  - `Target.cs` — логика объектов (очки/штраф, сила/вращение, VFX)
  - `Score.cs` — подсчёт очков и вывод в UI
  - `Lives.cs` — жизни и вывод в UI
  - `GameOver.cs` — потеря жизни при пропуске Good‑объекта
  - `Good.cs`, `Bad.cs` — маркерные компоненты типов объектов
- `Assets/MY FOLDER/prefabs/` — префабы еды, bad‑объекта и VFX

---

## 📞 Контакты

- Сайт: easypro.academy — https://easypro.academy/
- VK: https://vk.com/easypro.academy
- Telegram: https://t.me/easypro_channel
